<div data-controller="world" class="flex h-screen justify-between">

  <div class="w-72 p-5 shadow-lg">
    <%= link_to '🅗Taskete', dashboard_path, class: 'font-medium pb-3' %>
    <%= render "shared/sidebar_l" %>
  </div>

  <div class="flex-grow pt-4 pl-10 overflow-scroll">
    <div data-world-target="contentmain" data-controller = "workflow">
      <%= render "workflows/workflow", locals: {workflow: @workflow} %>
    </div>
  </div>

  <div class="w-60 pt-2 px-5">
    <%= render 'shared/sidebar_r' %>
  </div>

</div>
